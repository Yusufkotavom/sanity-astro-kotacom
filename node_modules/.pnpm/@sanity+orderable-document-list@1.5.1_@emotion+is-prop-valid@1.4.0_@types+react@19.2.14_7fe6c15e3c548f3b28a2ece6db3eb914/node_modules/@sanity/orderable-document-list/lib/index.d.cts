import {Component} from 'react'
import type {ComponentType} from 'react'
import type {ConfigContext} from 'sanity'
import {FieldDefinition} from 'sanity'
import {JSX as JSX_2} from 'react'
import {ListItem} from 'sanity/structure'
import {MenuItem} from 'sanity/structure'
import type {SanityClient} from '@sanity/client'
import type {SortOrdering} from 'sanity'
import {StringDefinition} from 'sanity'
import {StructureBuilder} from 'sanity/structure'
import type {ToastParams} from '@sanity/ui'

declare type NewItemPosition = 'after' | 'before'

export declare class OrderableDocumentList extends Component<OrderableDocumentListProps, State> {
  constructor(props: OrderableDocumentListProps)
  actionHandlers: {
    showIncrements: () => void
    resetOrder: () => Promise<void>
  }
  render(): JSX_2.Element | null
}

export declare function orderableDocumentListDeskItem(config: OrderableListConfig): ListItem

declare interface OrderableDocumentListProps {
  options: {
    type: string
    client: SanityClient
    filter?: string
    params?: Record<string, unknown>
    currentVersion?: string
  }
}

export declare interface OrderableListConfig {
  type: string
  id?: string
  title?: string
  icon?: ComponentType
  params?: Record<string, unknown>
  filter?: string
  menuItems?: MenuItem[]
  createIntent?: boolean
  context: ConfigContext
  S: StructureBuilder
}

export declare const orderRankField: (config: RankFieldConfig) => FieldDefinition<'string'>

export declare const orderRankOrdering: SortOrdering

export declare interface RankFieldConfig
  extends Partial<Omit<StringDefinition, 'name' | 'type' | 'initialValue'>> {
  type: string
  newItemPosition?: NewItemPosition
}

declare interface State {
  showIncrements: boolean
  resetOrderTransaction: ToastParams
}

export {}
